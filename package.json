{
  "name": "new-app",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "inspect": "bun run --watch --inspect src/index.ts",
    "app:build": "bun build --minify --outdir=out ./src/index.js ./src/index.ts"
  },
  "dependencies": {
    "@elysiajs/swagger": "^0.6.1",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}